<UserControl x:Class="Kable.UI.Wpf.CommTerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Kable.UI.Wpf"
             xmlns:loc="clr-namespace:Kable.UI.Wpf.Localization"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Background="#1E1E1E">
        <TabControl Background="#1E1E1E" BorderBrush="#3F3F46" BorderThickness="0,1,0,0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#252526"/>
                    <Setter Property="Foreground" Value="#CCCCCC"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="14,8"/>
                    <Setter Property="Margin" Value="0,0,2,0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#1E1E1E"/>
                            <Setter Property="Foreground" Value="#007ACC"/>
                            <Setter Property="BorderThickness" Value="0,2,0,0"/>
                            <Setter Property="BorderBrush" Value="#007ACC"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>

            <!-- 1. 수시 명령 콘솔 탭 (Aperiodic Commands) -->
            <TabItem Header="{loc:Loc Header_CommandConsole}">
                <local:CommandConsoleView DataContext="{Binding CommandConsole}"/>
            </TabItem>

            <!-- 2. 상시 텔레메트리 스트림 탭 (Periodic Telemetry) -->
            <TabItem Header="{loc:Loc Header_PeriodicTelemetry}">
                <local:TelemetryStreamView DataContext="{Binding TelemetryStream}"/>
            </TabItem>

            <!-- 3. 실시간 알람 탭 (Active Alarms) -->
            <TabItem Header="{loc:Loc Header_Alarms}">
                <local:AlarmListView DataContext="{Binding AlarmList}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
