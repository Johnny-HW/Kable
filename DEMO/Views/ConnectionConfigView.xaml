<UserControl x:Class="Kable.ConfigStudio.Views.ConnectionConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:models="clr-namespace:Kable.ConfigStudio.Models"
             xmlns:loc="clr-namespace:Kable.UI.Wpf.Localization;assembly=Kable.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
        <StackPanel MaxWidth="900" HorizontalAlignment="Stretch">
            <!-- 1. Device Info Card -->
            <Border Background="#1E1E2E" CornerRadius="16" Padding="20" Margin="0,0,0,16" BorderBrush="#313244" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.25" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="{loc:Loc Header_DeviceInfo}" FontWeight="Bold" FontSize="15" Foreground="#89B4FA" Margin="0,0,0,14"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Loc Lbl_DeviceId}" Foreground="#A6ADC8" VerticalAlignment="Center" FontWeight="Medium"/>
                        <TextBox Grid.Column="1" Text="{Binding Profile.DeviceName, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8" FontSize="13">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Loc Lbl_Description}" Foreground="#A6ADC8" VerticalAlignment="Center" FontWeight="Medium"/>
                        <TextBox Grid.Column="1" Text="{Binding Profile.Description, UpdateSourceTrigger=PropertyChanged}" 
                                 Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8" FontSize="13">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 2. Transport Selection Card -->
            <Border Background="#1E1E2E" CornerRadius="16" Padding="20" Margin="0,0,0,16" BorderBrush="#313244" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.25" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="{loc:Loc Header_TransportSelection}" FontWeight="Bold" FontSize="15" Foreground="#89B4FA" Margin="0,0,0,14"/>
                    
                    <!-- Transport Segmented Pill Radio Group -->
                    <Border Background="#252538" CornerRadius="14" Padding="6" Margin="0,0,0,16">
                        <WrapPanel Orientation="Horizontal">
                            <RadioButton Content="Serial" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsSerial, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.Serial}"/>
                            <RadioButton Content="TCP/IP" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsTcp, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.Tcp}"/>
                            <RadioButton Content="Named Pipe" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsNamedPipe, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.NamedPipe}"/>
                            <RadioButton Content="Modbus-TCP" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsModbusTcp, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.ModbusTcp}"/>
                            <RadioButton Content="Melsec SLMP" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsMelsecSlmp, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.MelsecSlmp}"/>
                            <RadioButton Content="MQTT" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsMqtt, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.Mqtt}"/>
                            <RadioButton Content="OPC UA" Style="{StaticResource TransportRadioBtn}" IsChecked="{Binding IsOpcUa, Mode=OneWay}" Command="{Binding SelectTransportCommand}" CommandParameter="{x:Static models:TransportType.OpcUa}"/>
                        </WrapPanel>
                    </Border>

                    <!-- Serial Specific Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsSerial, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,10">
                                <TextBlock Text="COM Port (Auto-Detected):" Foreground="#A6ADC8" VerticalAlignment="Center" FontWeight="Medium"/>
                                <Button Content="🔄 Refresh Ports" Command="{Binding RefreshComPortsCommand}" DockPanel.Dock="Right" Padding="12,6" Background="#313244" Foreground="#89B4FA" FontWeight="SemiBold" Cursor="Hand" BorderThickness="0"/>
                            </DockPanel>
                            <ComboBox Style="{StaticResource IosComboBoxStyle}" ItemsSource="{Binding DetectedComPorts}" SelectedItem="{Binding Profile.PortName}" Margin="0,0,0,10"/>

                            <UniformGrid Columns="2" Margin="0,4,0,0">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="BaudRate:" Foreground="#A6ADC8" Margin="0,0,0,4"/>
                                    <ComboBox Style="{StaticResource IosComboBoxStyle}" ItemsSource="{Binding AvailableBaudRates}" SelectedItem="{Binding Profile.BaudRate}"/>
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="Parity:" Foreground="#A6ADC8" Margin="0,0,0,4"/>
                                    <ComboBox Style="{StaticResource IosComboBoxStyle}" ItemsSource="{Binding AvailableParities}" SelectedItem="{Binding Profile.Parity}"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <!-- TCP Specific Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsTcp, Converter={StaticResource BoolToVis}}">
                        <UniformGrid Columns="2">
                            <StackPanel Margin="0,0,8,0">
                                <TextBlock Text="Destination Host / IP:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                <TextBox Text="{Binding Profile.HostIp, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8" FontSize="13"/>
                            </StackPanel>
                            <StackPanel Margin="8,0,0,0">
                                <TextBlock Text="TCP Port:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                <TextBox Text="{Binding Profile.TcpPort, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8" FontSize="13"/>
                            </StackPanel>
                        </UniformGrid>
                    </Border>

                    <!-- Modbus-TCP Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsModbusTcp, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <UniformGrid Columns="2" Margin="0,0,0,10">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="Modbus Server IP:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.HostIp, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="TCP Port (Default 502):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.TcpPort, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                            </UniformGrid>
                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="Unit ID (Slave ID):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.ModbusUnitId, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="Test Register Address:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.ModbusTestRegister, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <!-- Melsec SLMP Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsMelsecSlmp, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <UniformGrid Columns="2" Margin="0,0,0,10">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="PLC Host IP (Q/iQ-R):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.HostIp, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="SLMP Port (Default 5000):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.TcpPort, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                            </UniformGrid>
                            <UniformGrid Columns="3">
                                <StackPanel Margin="0,0,6,0">
                                    <TextBlock Text="Network No:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.MelsecNetworkNo, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="4,0,4,0">
                                    <TextBlock Text="PC No (Default 255):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.MelsecPcNo, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="6,0,0,0">
                                    <TextBlock Text="Target Device (e.g. D1000):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.MelsecDevice, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <!-- MQTT Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsMqtt, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <UniformGrid Columns="2" Margin="0,0,0,10">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="MQTT Broker Host:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.HostIp, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="Port (Default 1883):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.TcpPort, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                            </UniformGrid>
                            <UniformGrid Columns="2">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="Client ID:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.MqttClientId, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="Topic Prefix:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                                    <TextBox Text="{Binding Profile.MqttTopicPrefix, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <!-- OPC UA Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsOpcUa, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="OPC UA Endpoint URL:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Profile.OpcEndpointUrl, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8" Margin="0,0,0,10"/>
                            <TextBlock Text="Target NodeId (e.g. ns=2;s=Device.Status):" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Profile.OpcNodeId, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                        </StackPanel>
                    </Border>

                    <!-- Named Pipe Settings -->
                    <Border Background="#252538" CornerRadius="12" Padding="16" Visibility="{Binding IsNamedPipe, Converter={StaticResource BoolToVis}}">
                        <StackPanel>
                            <TextBlock Text="Local Named Pipe Identifier:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Profile.PipeName, UpdateSourceTrigger=PropertyChanged}" Background="#313244" Foreground="#CDD6F4" BorderThickness="0" Padding="10,8"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 3. Codec & Router Settings Card -->
            <Border Background="#1E1E2E" CornerRadius="16" Padding="20" Margin="0,0,0,16" BorderBrush="#313244" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.25" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="{loc:Loc Header_CodecRouter}" FontWeight="Bold" FontSize="15" Foreground="#89B4FA" Margin="0,0,14"/>
                    <UniformGrid Columns="2">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Framing Codec:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                            <ComboBox Style="{StaticResource IosComboBoxStyle}" ItemsSource="{Binding AvailableCodecs}" SelectedItem="{Binding Profile.Codec}"/>
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="Transaction Router:" Foreground="#A6ADC8" Margin="0,0,0,6"/>
                            <ComboBox Style="{StaticResource IosComboBoxStyle}" ItemsSource="{Binding AvailableRouters}" SelectedItem="{Binding Profile.Router}"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
