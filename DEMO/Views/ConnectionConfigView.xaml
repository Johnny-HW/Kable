<UserControl x:Class="Kable.ConfigStudio.Views.ConnectionConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Kable.UI.Wpf.Controls;assembly=Kable.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
        <StackPanel MaxWidth="900" HorizontalAlignment="Stretch">
            <!-- 1. Reusable Device Profile Editor -->
            <controls:DeviceProfileEditorView Margin="0,0,0,16"/>

            <!-- 2. Reusable Multi-Transport Configuration View -->
            <controls:TransportConfigView Margin="0,0,0,16"/>

            <!-- 3. Reusable Codec & Router Configuration View -->
            <controls:CodecRouterConfigView Margin="0,0,0,16"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
