<UserControl x:Class="Kable.ConfigStudio.Views.LiveInspectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Kable.UI.Wpf;assembly=Kable.UI.Wpf"
             xmlns:controls="clr-namespace:Kable.UI.Wpf.Controls;assembly=Kable.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <!-- 1. 수시 명령 및 파라미터 제어 뷰 (상단) -->
            <RowDefinition Height="3*" MinHeight="250"/>
            <!-- 2. 그리드 스플리터 -->
            <RowDefinition Height="6"/>
            <!-- 3. 실시간 전체 원본 패킷 스트림 로그 (하단) -->
            <RowDefinition Height="2*" MinHeight="180"/>
        </Grid.RowDefinitions>

        <!-- 1. 수시 명령 콘솔 -->
        <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.04" Color="#000000"/>
            </Border.Effect>
            <ui:CommandConsoleView DataContext="{Binding Terminal.CommandConsole}"/>
        </Border>

        <!-- 2. 높이 조절 스플리터 -->
        <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" Background="#E2E8F0" ResizeDirection="Rows" Cursor="SizeNS"/>

        <!-- 3. 통신 원본 로그 스트림 (Raw TX/RX 패킷 + Hex Dump + 로그 저장) -->
        <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.04" Color="#000000"/>
            </Border.Effect>
            <controls:RawPacketLogStreamView DataContext="{Binding Terminal.TelemetryStream.RawLogStream}"/>
        </Border>
    </Grid>
</UserControl>
