<UserControl x:Class="Kable.ConfigStudio.Views.AlarmMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="clr-namespace:Kable.UI.Wpf;assembly=Kable.UI.Wpf"
             xmlns:loc="clr-namespace:Kable.UI.Wpf.Localization;assembly=Kable.UI.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="4">
        <!-- Spontaneous Alarm Viewport -->
        <Border Background="#1E1E2E" CornerRadius="16" BorderBrush="#313244" BorderThickness="1" ClipToBounds="True">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.25" Color="#000000"/>
            </Border.Effect>
            <ui:AlarmListView DataContext="{Binding Terminal.AlarmList}"/>
        </Border>
    </Grid>
</UserControl>
